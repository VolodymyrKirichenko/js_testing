// Given an array of strings, write a function that returns the longest common prefix
// shared by all strings in the array. If there is no common prefix, return an empty string "".

//Requirements
// The function should accept an array of strings.
//
// It should return a string — the longest common prefix.
//
// If there is no common prefix, return an empty string.

const longestCommonPrefix = (strs) => {

}


// expect(longestCommonPrefix(["flower", "flow", "flight"])).toBe("fl");
// expect(longestCommonPrefix(["dog", "racecar", "car"])).toBe("");
// expect(longestCommonPrefix(["alone"])).toBe("alone");
// expect(longestCommonPrefix([])).toBe("");


module.exports = longestCommonPrefix;